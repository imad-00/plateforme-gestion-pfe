# PFE Management Platform - Sprint 0

Ce repository contient la base technique du backend Django/DRF pour la plateforme de gestion des PFE.

Le setup Sprint 0 est documente ici:
- [`backend/README.md`](backend/README.md)

Demarrage rapide:
```bash
cp backend/.env.example backend/.env
docker compose up --build
```
